version: 1.0.0
# This should match the app - package name in your setup.py
name: mch_bridge

requirements:
  skip: true
  conda:
    channels:
      - conda-forge
    packages:
      - aiomysql
      - pymysql
  pip:

post:
